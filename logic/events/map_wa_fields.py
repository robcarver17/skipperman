import pandas as pd

def map_wa_fields_in_df(wa_as_df: pd.DataFrame,
                        wa_mapping_dict: dict) -> pd.DataFrame:

    ## Return dataframe with new columns; but don't map non existent

    return mapped_wa_data
